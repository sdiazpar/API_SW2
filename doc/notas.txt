aspectos importantes:

listado total te devuelva total, page, limit, [_id, shape, city, _]
lo que te filtra te lo tiene que devolver: o eliminar query parameters
añadir parametro next a openapi

Hacer:

rober: GET /sightings: Next bien, que sea el siguiente y empiece a partirr de ese. y QUERYS 
rober: GET /locations Conectar con Google Maps (geolocalization)
alejandro: GET /sightings/{sightingsId} -> METER TRYCATCH probar en Postman: localhost:3000/sightings/68205232099a58e0d86bdace
alejandro: POST /sightings: meter el CORREO del usuario y ajv
sergio terminado: DELETE /sightings/{sightingsId}:
miguel: UPDATE /sightings/{sightingsId} añadirlo en openapi porque no estña hecho
truji: GET /people
truji: POST /people: ajv

CAMBIOS EN OPENAPI para que se corresponda con el codigo


// Comandos para arrancar
node loadCsv.js
node foreingKeys.js
npm start




// Demo
http://localhost:3000/sightings + x-api-key (GET)
http://localhost:3000/sightings + meter en body un ejemplo de la inserción (POST)
http://localhost:3000/sightings/682e020a9026c1b5cf83ccca + x-api-key (GET)
http://localhost:3000/sightings/68166a1c9386957070cc0796 + meter en body lo que se quiere cambiar
http://localhost:3000/sightings/68166a1c9386957070cc0797 + meter el ID a borrar

El resto trufi