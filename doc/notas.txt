aspectos importantes:

listado total te devuelva total, page, limit, [_id, shape, city, _]
lo que te filtra te lo tiene que devolver: o eliminar query parameters
añadir parametro next a openapi

Hacer:
Next bien, que sea el siguiente y empiece a partirr de ese.
GET /locations Conectar con Google Maps (geolocalization)
GET /sightings/{sightingsId} -> no le hace falta paginacion, nada que paginar. Ni limitar
POST /sightings
DELETE /sightings
GET /people
POST /people




node loadCsv.js
node foreingKeys.js